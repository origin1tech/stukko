/* globals describe:false, it:false */

'use strict';

var chai = require('chai'),
    expect = chai.expect,
    should = chai.should,
    assert = chai.assert;

describe('UAuth Tests', function() {

    it('should', function() {
        assert.equal(true, true);
    });

});